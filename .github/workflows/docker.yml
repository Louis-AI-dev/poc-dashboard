name: Build and Push Docker image

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    permissions:
      packages: write
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: louis-ai-dev
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Docker image
        run: |
          docker build -t ghcr.io/louis-ai-dev/poc-dashboard:${{ github.sha }} .
          docker tag ghcr.io/louis-ai-dev/poc-dashboard:${{ github.sha }} ghcr.io/louis-ai-dev/poc-dashboard:latest

      - name: Push Docker image
        run: |
          docker push ghcr.io/louis-ai-dev/poc-dashboard:${{ github.sha }}
          docker push ghcr.io/louis-ai-dev/poc-dashboard:latest
